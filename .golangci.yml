version: 2

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - revive
    - misspell

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 3m
