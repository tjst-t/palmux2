{
  "entities": [
    {
      "name": "Repository",
      "definition": "ghq で管理され、palmux に Open されたリポジトリ。primary worktree (IsPrimary) を必ず1つ持つ",
      "identity": "repoId (owner--repo--hash4)",
      "lifecycle": ["opened", "closed"],
      "owns": ["Workspace"],
      "notes": "真実の所在は repos.json。palmux 独自の状態は持たない"
    },
    {
      "name": "Workspace",
      "definition": "git worktree path を identity とする作業単位。旧称 Branch。path が同じ限り git checkout で HEAD が変わっても identity は不変",
      "identity": "worktree path 由来の workspaceId (domain.WorkspaceSlugIDFromPath)",
      "lifecycle": ["opened", "head_changed (0回以上)", "closed"],
      "owns": ["TabSet"],
      "notes": "role 属性 (WorkspaceRole 参照) を持つ: primary Workspace は常に role=control、dispatch で作られた Workspace は role=worker。role は identity ではなく分類用の属性で、既存の IsPrimary 判定とは独立して扱う (将来 primary 以外にも control 相当が生まれる可能性を閉じないため)"
    },
    {
      "name": "TabSet",
      "definition": "Workspace が持つタブの集合。タブは Provider が生成し、順序は登録順",
      "identity": "Workspace に従属 (単独の identity なし)",
      "lifecycle": [],
      "owns": ["Tab"],
      "notes": "Claude / Files / Git / Docs は protected、Bash のみ複数可。Docs タブは ADR-0010 で Sprint タブから改称され、Conditional 表示条件も docs/ROADMAP.json の存在から汎用ドキュメント投影ファイル (docs/**/*.doc.json) の存在に変わる"
    },
    {
      "name": "Document",
      "definition": "汎用ドキュメントスキーマ (kind/title/status/body/meta/tags/links/children の再帰ノード) に準拠した投影ファイル。Docs タブが表示する対象",
      "identity": "リポジトリ内のファイルパス",
      "lifecycle": ["detected", "updated", "removed"],
      "owns": [],
      "notes": "palmux は Document を所有しない — 出力側 (claude-skills 等) が真実の所在で、palmux は読み取り専用のレンダラーに徹する。palmux は kind / meta / tags / links.rel の値集合を一切解釈せず、レイアウトは出力側が所有する ViewTemplate に委ねる (ADR-0010 / ADR-0011)。スキーマ契約は DESIGN/data.json"
    },
    {
      "name": "SprintClaim",
      "definition": "ある Sprint (claude-skills の ROADMAP.json 上の ID) が、どの Worker Workspace で dispatch 中かを示す palmux 内部の追跡レコード",
      "identity": "sprintId",
      "lifecycle": ["dispatched", "in_progress", "blocked", "done", "cleared"],
      "owns": [],
      "notes": "Sprint そのものは claude-skills の成果物 (ROADMAP.json) であり palmux は所有しない。SprintClaim は『どこで誰が触っているか』を追跡する palmux 側の調整用メタデータに過ぎず、一次情報のミラーではない (ADR-0006)。単一ホスト内の palmux インスタンスに閉じたスコープで、複数ホストにまたがる同期は対象外 (ADR-0006 notes 参照)"
    }
  ],
  "value_objects": [
    {
      "name": "Block",
      "definition": "Document の中身のレイアウト要素。heading / statusBadge / progressBar / table / timeline / tree / card / keyValueGrid / panel / decisionList / taskList / linkList / markdown / section の固定語彙 (+ 子要素専用の decisionItem / taskItem)。語彙は palmux 本体のコードが持つ固定集合で、出力側は勝手に増やせない (ADR-0011)"
    },
    {
      "name": "ViewTemplate",
      "definition": "kind → Block 構成のマッピング。出力側 (claude-skills 等) が所有・出力する。所有範囲は『1ドキュメントの中身のレイアウト』のみで、ナビゲーション・一覧・検索の枠は palmux 本体が持つ (ADR-0011)"
    },
    {
      "name": "WorkspaceRole",
      "definition": "control | worker。Workspace の分類属性。control = 常に main を HEAD に保ち計画・dispatch のみ行う (ghq primary が該当)。worker = dispatch されて実装を行う (gwq worktree が該当)"
    }
  ],
  "relationships": [
    {
      "from": "Repository",
      "to": "Workspace",
      "kind": "one_to_many",
      "label": "1 Repository は複数の Workspace (primary 1 + linked worktree N) を持つ"
    },
    {
      "from": "Workspace",
      "to": "TabSet",
      "kind": "one_to_one",
      "label": "1 Workspace は1つの TabSet を持つ"
    },
    {
      "from": "Workspace",
      "to": "SprintClaim",
      "kind": "one_to_one",
      "label": "role=worker の Workspace は同時に最大1件の active SprintClaim を持つ。historical claim は保持しない (cleared で消える)"
    },
    {
      "from": "Workspace",
      "to": "Document",
      "kind": "one_to_many",
      "label": "1 Workspace の worktree 内に 0 個以上の Document (投影ファイル) が存在しうる。1つ以上あるとき Docs タブが Conditional に生成される"
    },
    {
      "from": "Document",
      "to": "ViewTemplate",
      "kind": "many_to_one",
      "label": "Document.kind で ViewTemplate を索く。対応する template が無い kind も汎用 fallback で表示できる"
    }
  ],
  "glossary": [
    {
      "term": "Docs タブ",
      "definition": "旧 Sprint タブ (ADR-0010 で改称)。汎用ドキュメントスキーマに準拠した投影ファイルを、出力側が所有する view template に従ってレンダリングする Conditional タブ。claude-skills 固有の JSON 形式 (ROADMAP.json 等) を直接解釈しない"
    },
    {
      "term": "投影ファイル",
      "definition": "claude-skills 等が自分の内部形式 (ROADMAP.json 等) とは別に、汎用ドキュメントスキーマ準拠で出力するファイル。内部形式はそのまま維持してよく、契約を満たすのは投影の側 (ADR-0010)"
    },
    {
      "term": "Control Plane",
      "definition": "role=control の Workspace (通常 ghq primary)。常に main を HEAD に保ち、計画・ロードマップ更新・Sprint の dispatch 判断のみを行う。実装は行わない"
    },
    {
      "term": "Worker",
      "definition": "role=worker の Workspace。dispatch によって作られた gwq worktree で、1 Sprint = 1 Worker Workspace が基本単位。実装を行う"
    },
    {
      "term": "Dispatch",
      "definition": "Control Plane が Worker Workspace を作成し (gwq worktree 作成 + Claude タブ自動起動)、初期プロンプトを注入する一連の操作"
    },
    {
      "term": "Claim",
      "definition": "SprintClaim エンティティのカジュアルな呼び方。「この Sprint は claim されている (dispatch 済みで誰かが触っている)」という状態を指す"
    },
    {
      "term": "claude-skills",
      "definition": "palmux2 とは別リポジトリ (github.com/tjst-t/claude-skills) の汎用 Claude Code スキル集 (autopilot / sprint / design 等)。palmux2 の一部ではなく、他プロジェクトでも使われる再利用可能な独立プロダクト。palmux はこれに依存せず、汎用 API と注入 Skill を介して疎結合に連携するのみ (ADR-0008)"
    }
  ]
}
