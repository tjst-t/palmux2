# Skill Retrospective — Release-blocker fix milestone (Sprint Sc4f091)

_Generated at milestone arrival. Failure/rework → SKILL diff, or an explicit reason not to._

**Signals this batch:** 3 (compromises 0, overlooked/nested-notification-routing gaps 2, ungrounded decisions 2/13 ≈ 15%); concerns empty rate high again (see #3) — carrying forward a Deferred item from Sfa2bab

| # | Signal (source) | What happened | Root cause | Diff proposal — or reason for deferral |
|---|---|---|---|---|
| 1 | process pattern · orchestrator-level, recurring across ~6+ agent turns this Sprint | The Sc4f091-2 implement agent ended its own turn on a passive wait ("waiting for run N", "waiting for the controlled experiment") **six separate times** across this one Story, despite explicit instructions (carried over from the Sfa2bab retrospective's own proposed fix) to poll actively and never wait passively. Each time, the orchestrator had to directly inspect host state (`ps`, `incus list`) and manually confirm the bounded `timeout`-wrapped process had already exited before resuming the agent. | **SKILL/harness defect, confirmed recurring** — this is the exact signal Sfa2bab's retrospective (#1/#6) already diagnosed and proposed a diff for, and it recurred in the very next Sprint essentially unchanged, even after the implement-agent prompt explicitly restated the rule inline (not just relying on a canonical SKILL file). The instruction to "poll yourself, never passively wait" is evidently not sticking even when stated directly in-prompt — suggesting the fix needs to be structural (e.g. the harness itself enforcing a max idle-turn budget before forcing a status check) rather than purely instructional. | **This is now a 2nd-occurrence Deferred item** (see below) — escalating rather than re-proposing the same textual fix a third time. Recommend the next iteration try a structural mitigation (harness-level bounded-idle detection) rather than another instruction restatement, since instruction-restatement already failed once. |
| 2 | overlooked · nested sub-agent notification routing (NEW manifestation of a related but distinct gap) | The Sc4f091-2 code-review sub-agent spawned its OWN nested sub-agent ("Angle C", a cross-file tracer) to help answer one of its scrutiny points, then itself ended its turn waiting for that nested agent's completion. When "Angle C" finished, its notification did not reach the review agent (which had already gone idle) — it was delivered directly to the orchestrator instead, nearly two hours later, with no signal in between that anything was stuck. The orchestrator only caught this because the user asked "is it really still reviewing?" and a manual transcript-timestamp check showed ~2 hours of silence. | **SKILL/harness defect, distinct from #1** — #1 is "agent waits on ITS OWN spawned background OS process"; this is "agent waits on ITS OWN spawned NESTED AGENT, and the notification-delivery mechanism doesn't reliably route back to a parent that has already gone idle." Sprint-level SKILL docs don't yet address agent-delegates-to-agent chains at all — only single-level agent-to-orchestrator patterns are covered. A competent agent following current SKILL text literally would still hit this, since nothing warns against nested delegation without an active-wait budget. | Propose: add explicit guidance to `sprint-run.md`/`sprint-verify.md` (or a new shared reference) discouraging review/verify sub-agents from spawning their OWN nested sub-agents and then passively waiting — if a sub-question genuinely needs a separate investigative pass, the TOP-LEVEL orchestrator should spawn it directly (flat, not nested), exactly the same reasoning `autopilot-start.md` already applies to why Story implementation must not be wrapped in a single Sprint-level sub-agent. Also worth an explicit note that this session's own user caught what the harness didn't — a "how long has this agent actually been silent" sanity check might be worth normalizing as something the orchestrator does periodically for long-running background agents, not just when asked. |
| 3 | concerns-sensor empty rate · still ~100% this batch | Same observation as Sfa2bab's retrospective #5 — no implement/review agent this batch returned a distinctly-labeled `concerns[]` item (the "AC met, in scope, but something feels off" shape); everything was either a clear defect (routed to backlog/fix) or a load-bearing decision (routed to escalation). | Unresolved from last batch — the proposed mitigation (isolate the concerns[] ask as its own single-purpose field in agent prompts) was not actually tried this batch, since these prompts were bug-fix-shaped rather than feature-shaped and didn't re-include that exact ask. | Deferral, still open: try the isolated-concerns[]-field format specifically on the NEXT feature-shaped (not bug-fix-shaped) Sprint, since a fix-only Sprint may genuinely have less occasion to surface "off but technically fine" observations than a feature-building one. |
| 4 | ungrounded decisions · 2/13 (~15%) | Two decisions lacked a VISION/DESIGN_PRINCIPLES reference. | Below the 30% warning threshold. | No diff. |

## Deferred (carried from prior milestones)

- **#1 above is a 2nd-occurrence deferral of Sfa2bab's skill-retrospective signal #1/#6** ("sub-agents stall mid-wait on self-spawned background work"). The Sfa2bab-batch proposal (restate the rule more explicitly in-prompt) was tried this batch and demonstrably did not fully resolve the pattern (it happened 6 times in one Story despite explicit inline instructions). Per the autopilot SKILL's own stall criterion ("失敗が SKILL に還元されない状態が 2 sprint 以上続いたら報告"), **this is now flagged to the PO as a stalling loop** — instruction-only fixes have been tried and are insufficient; a structural/harness-level mitigation should be considered next, not a third repetition of the same textual reminder.
