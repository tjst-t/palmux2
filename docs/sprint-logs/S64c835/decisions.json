{
  "sprint": "S64c835",
  "title": "no-halt-agent hardening — races + test hygiene + agent orphan-GC",
  "decisions_S64c835_1": [
    {
      "timestamp": "2026-07-12T00:00:00Z",
      "category": "verify",
      "title": "Reproduced both pre-existing races before fixing (AC-S64c835-1-1/2)",
      "detail": "go test -race ./internal/tab/claudetui/ -count=1 on the base commit reproduced 29 identical DATA RACE reports, all charmbracelet/x/vt (*Emulator).Close() writing its internal `closed` bool (emulator.go:265) racing (*Emulator).Read() reading it (emulator.go:252), reached via daemon.go's spawnWithArgs drainer goroutine (Read, daemon.go:820) vs teardown (Close, daemon.go:1413). Separately reproduced roleCoordinator.OnUnsubscribe: a write to rc.subs at role.go:155 (under rc.mu) racing an UNGUARDED read of len(rc.subs) at role.go:172 in a DIFFERENT concurrent OnUnsubscribe call — the debug-log line ran len(rc.subs) *after* rc.mu.Unlock()."
    },
    {
      "timestamp": "2026-07-12T00:00:00Z",
      "category": "design",
      "title": "roleCoordinator fix: capture counts under the lock (AC-S64c835-1-2)",
      "detail": "OnSubscribe and OnUnsubscribe both had the same bug shape: len(rc.subs) read for a slog.Debug call *after* rc.mu.Unlock(). Fixed both by capturing the count into a local variable while still holding rc.mu, then using that local in the (now-unlocked) log call. No behavior change, no new locking, no deadlock risk — pure hoist of a read to inside an already-held critical section. Added TestRoleCoordinatorConcurrentSubscribeUnsubscribe (32 workers x 50 rounds of Subscribe/TakeActive/Unsubscribe) as a regression test; verified it fails under -race with role.go reverted to its old form and passes with the fix."
    },
    {
      "timestamp": "2026-07-12T00:00:00Z",
      "category": "design",
      "title": "Emulator fix: attempted palmux2-side mutex first, proved it cannot work",
      "detail": "First attempt: added a closeMu to internal/tab/claudetui/Emulator, held by Read() for its entire (potentially indefinite) call, with Close() using closeMu.TryLock() to avoid deadlocking against a blocked Read(). This still raced under -race even with an increasingly generous bounded retry (tried 0ms and 200ms budgets with runtime.Gosched() spin). Root cause understood after the second failure: go test -race's race detection is based purely on the presence/absence of a happens-before edge (an actual synchronization primitive shared between the two accesses), not on wall-clock proximity — waiting longer before Close()'s unsynchronized fallback call does not create that edge, so the race is reported regardless of how much real time separates the two accesses. A mutex that Read() holds for its full (unboundedly long) call cannot be shared with Close() without either deadlocking (Close() waiting on a lock a permanently-blocked Read() holds) or leaving this exact un-closeable gap. Concluded this is provably unsolvable via palmux2-side wrapping alone, given io.PipeReader (which the underlying `closed` bool guards) offers no deadline/cancel support and vt.Emulator's pr/pw fields are unexported."
    },
    {
      "timestamp": "2026-07-12T00:00:00Z",
      "category": "design",
      "title": "Emulator fix: root-cause patch via local module replace (AC-S64c835-1-1)",
      "detail": "Since the actual bug is charmbracelet/x/vt's Emulator.closed being a plain (unsynchronized) bool touched by both Close() and Read()/Write() from different goroutines, and SafeEmulator (which claims thread-safety) does not fix this (Close isn't overridden at all; Read explicitly bypasses SafeEmulator's own mutex) — patched a local copy of the exact same module version at third_party/charmbracelet-x-vt-racefix, changing only Emulator.closed from `bool` to `atomic.Bool` (3 call sites: 2 Load, 1 CompareAndSwap replacing the old read-then-write). Wired in via a `replace` directive in go.mod (scoped, documented, minimal — no other files touched, test files stripped from the copy). io.PipeReader.Read/io.PipeWriter.CloseWithError (what Close ultimately calls) are already safe for concurrent use per io.Pipe's own design, so this is the complete, root-cause fix — no additional palmux2-side locking needed. Simplified internal/tab/claudetui/Emulator.Close()/Read() back to plain pass-throughs. This departs from the story's 'serialize on palmux2's side' framing, but that framing did not anticipate that Read() can block indefinitely while Close() must interrupt it — a combination proven unsolvable with pure caller-side synchronization; the decisions above document why. Backlog item 'charmbracelet/x/vt Emulator.Close vs Read' should be closed by dropping the replace directive once upstream ships an equivalent fix."
    },
    {
      "timestamp": "2026-07-12T00:00:00Z",
      "category": "verify",
      "title": "Regression tests proven to catch both original bugs",
      "detail": "Added TestEmulatorCloseRacesReadAndFeed (20 iterations, mirrors daemon.go's drainer/readLoop/teardown goroutine triple) and TestRoleCoordinatorConcurrentSubscribeUnsubscribe. Confirmed each fails under -race when its corresponding fix is reverted in isolation (git stash on role.go alone; git stash on go.mod alone to restore the unpatched vt module) and passes with the fix restored. Also incidentally caught a related, previously-undocumented Feed()-vs-Close() race on the same `closed` field (SafeEmulator.Write takes se.mu but Close bypasses it entirely) — fixed by the same atomic.Bool patch."
    },
    {
      "timestamp": "2026-07-12T00:00:00Z",
      "category": "verify",
      "title": "Final verification (AC-S64c835-1-1/2/3)",
      "detail": "go build ./... clean. go vet ./... clean. go test ./internal/tab/claudetui/ (non-race) PASS. go test -race ./internal/tab/claudetui/ -count=3 -v: 300/300 PASS, 0 DATA RACE reports. Repeated -count=3 -race run concurrently with `stress-ng --cpu 8` for ~110s (simulating a loaded CPU per the AC wording): still 0 races. PALMUX_SURVIVAL_SMOKE=1 go test ./internal/tab/claudetui/ -run TestParallelInstances -count=1: PASS, no regression. No leaked fake_claude/ptyhost processes after any run (verified via pgrep)."
    }
  ],
  "decisions_S64c835_2": [
    {
      "timestamp": "2026-07-12T12:00:00Z",
      "category": "design",
      "title": "AC-S64c835-2-1: deterministic ptyhost reap for TestParallelInstances_NeverClaimOrGCEachOther",
      "detail": "The test's real detached ptyhost launches (hostA1/hostB1/hostA2/hostB2, spawned via the production systemd-run/setsid Launcher) previously relied on scattered t.Cleanup(shutdownIsolationHost) calls that fire-and-forget a SHUTDOWN frame with no confirmation the underlying OS process actually exited (backlog #4: ~63 leaked ptyhosts observed under repeated/loaded runs). Replaced with a `launched []isolationHost` tracking slice + a single reapAndAssertNoLeak cleanup registered FIRST (so — t.Cleanup is LIFO — it runs LAST, after mgrA/mgrB.ShutdownAll and the mid-test GCOrphans calls have already had their chance). It: (1) sends SHUTDOWN to every tracked host, (2) polls each host's captured child pid (pidAlive, already used by discover.go) for exit with a 5s grace window, (3) force-SIGKILLs any survivor as a deterministic backstop and asserts (t.Errorf, not a swallow) if even that fails, (4) independently pgreps for both the test's uniquely t.TempDir()-pathed realBin and fakeClaudeBin — catching a leaked ptyhost SUPERVISOR process too, since only the CHILD's pid comes back over HELLO, never the supervisor's own pid. Both bins live under a t.TempDir() unique to this run, so a pgrep match can only be a process this specific test invocation spawned (never a concurrently running same-named test, a real host palmux2, or anything else on the box)."
    },
    {
      "timestamp": "2026-07-12T12:00:00Z",
      "category": "verify",
      "title": "AC-S64c835-2-1 verification",
      "detail": "PALMUX_SURVIVAL_SMOKE=1 go test ./internal/tab/claudetui/ -run TestParallelInstances_NeverClaimOrGCEachOther -count=3 -v: 3/3 PASS. pgrep -af fake_claude / pgrep -f 'palmux ptyhost' after each run: 0 real matches (only the invoking shell's own command line self-matched the grep pattern, confirmed by inspecting the matched pid = the pgrep-issuing shell itself)."
    },
    {
      "timestamp": "2026-07-12T12:00:00Z",
      "category": "verify",
      "title": "AC-S64c835-2-2: initial adaptive-timeout approach, then empirical discovery it wasn't sufficient",
      "detail": "First pass: added permTestTimeout(base) — a helper scaling a base duration by a raceTimeoutMultiplier (3 under `go test -race`, 1 otherwise, selected via a `//go:build race` / `//go:build !race` file pair so cmd/go's automatic race build tag picks the right constant with zero runtime detection) times an optional PALMUX_TEST_TIMEOUT_SCALE env multiplier — and replaced every hardcoded `2*time.Second` wait budget across bash_perm_integration_test.go / ask_integration_test.go / plan_integration_test.go (not just the 3 AC-named tests — all tests in those 3 files share the identical 'resolve on a goroutine, wait on a channel+time.After select' shape, so leaving some hardcoded while others were adaptive would have been inconsistent and left latent flake sources unaddressed). Verified this DID reduce flake frequency under `stress-ng --cpu 8` + `-race` (6/150 failures at 3x -> down after bumping to 15x), but even a 30s deadline (15x multiplier) STILL occasionally failed under heavy load, including under plain AMBIENT dev-box load with NO added stress-ng (6/450 failures at `go test -race -count=50`, ALL exactly at the full timeout with a bimodal instant-or-full-timeout distribution — not the graduated latency spread a pure scheduling-delay explanation would predict). This was the signal that timeout-tuning alone was treating a symptom, not the cause — escalated to root-cause investigation rather than declaring backlog #6 'fixed' by a bigger number (CLAUDE.md: fail はまず自分で直す / don't self-judge-suppress)."
    },
    {
      "timestamp": "2026-07-12T12:00:00Z",
      "category": "design",
      "title": "AC-S64c835-2-2: root cause was two real, pre-existing TOCTOU races in claudeagent's permission-waiter plumbing, not test flakiness",
      "detail": "Reproduced deterministically via `go test -race -timeout 3s` looped until Go's own test-timeout watchdog fired (a tighter external deadline than the test's own select, so GOTRACEBACK dumps ALL goroutine stacks BEFORE the test's own t.Fatal can fire) plus temporary fmt.Fprintf(os.Stderr,...) instrumentation in registerPermWaiter/AnswerAskQuestion/AnswerPermission logging permID + channel pointer + `ok`. Found: (1) handleCanUseTool/RequestPermission/requestAskAnswer/requestPlanResponse all broadcast the permission-identifying WS event (exposing permID to any listener) BEFORE awaitPermission's original implementation ever registered the response channel in a.permWaiters — a fast-enough Answer* caller (a test's own goroutine, or, under load, a real user's fast click) could call Answer*/AnswerAskQuestion/AnswerPlanResponse before any waiter existed; those paths do a plain map lookup+delete with `ok=false` on a miss and NO error, silently dropping the response. (2) After registering the waiter earlier (before the broadcast) with a first-pass fix, awaitPermission's OWN body ALSO called the same registerPermWaiter helper AGAIN, re-deriving the channel via a fresh map lookup instead of reusing the one its caller already obtained — captured by debug instrumentation in an actual repro: `registerPermWaiter permID=perm_3 ch=0xc000243500 new=true` (caller's registration) -> `AnswerAskQuestion permID=perm_3 ok=true ch=0xc000243500` (Answer* found it, deleted it, sent the response into THAT channel) -> `registerPermWaiter permID=perm_3 ch=0xc000243570 new=true` (awaitPermission's own re-lookup, now AFTER the entry was already consumed-and-deleted, so it silently creates a SECOND, orphaned channel and blocks on THAT forever) -> goroutine dump confirmed the CLI-side goroutine genuinely parked at manager.go's awaitPermission select with nothing left to ever wake it — a real, permanent lost-wakeup, not a slow one. This explains the bimodal instant-or-full-timeout signature exactly: no race hit -> instant; race hit -> hang forever (only awaitPermission's original 15-MINUTE fallback would have eventually unblocked it in production, matching the described real-world symptom of a permission click that silently does nothing)."
    },
    {
      "timestamp": "2026-07-12T12:00:00Z",
      "category": "design",
      "title": "AC-S64c835-2-2: actual fix — register once, thread the channel explicitly, never re-derive",
      "detail": "awaitPermission's signature changed from `awaitPermission(permID string)` to `awaitPermission(permID string, ch chan canUseToolResponse)` — it no longer calls registerPermWaiter itself at all. All 4 call sites (handleCanUseTool, RequestPermission, requestAskAnswer, requestPlanResponse) now capture `waiterCh := a.registerPermWaiter(permID)` immediately after minting permID and BEFORE any broadcast/publish call, and pass that exact channel object through to awaitPermission. This closes BOTH races structurally: the waiter unconditionally exists before permID is ever exposed (closes race 1), and there is only ever ONE call site that creates the channel for a given permID — no second, later, independent lookup can ever construct a competing orphaned channel (closes race 2). This is a genuine correctness fix to pre-existing production code (present since well before S64c835, unrelated to this Sprint's ptyhost/orphan-GC work), not a test-only workaround. Debug instrumentation was removed after the root cause was confirmed and the fix verified; the multiplier `raceTimeoutMultiplier` was reset back down to 3 (its original, modest value) since the actual hang is now eliminated and no longer needs a large timeout to paper over it — the adaptive-timeout mechanism from the first pass is KEPT (a genuinely reasonable, ordinary hardening for real scheduling variance under `-race`+load), just no longer load-bearing for this specific bug."
    },
    {
      "timestamp": "2026-07-12T12:00:00Z",
      "category": "verify",
      "title": "AC-S64c835-2-2 final verification",
      "detail": "go build ./... / go vet ./... clean. go test ./internal/tab/claudeagent/ (non-race) PASS. go test -race ./internal/tab/claudeagent/ -run 'TestBashPerm_NoLeakOverManyRounds|TestAskUserQuestion_FullRoundTrip|TestExitPlanMode_FullRoundTrip' -count=100..200 under both plain and stress-ng-loaded conditions: 0 failures post-fix (vs the repeatable 1-6% failure rate pre-fix at identical loads). Extended repro loop (`go test -race -timeout 3s -count=1` looped hundreds of times specifically hunting the tight `panic: test timed out` window that previously reproduced in single-digit percent of runs): 0 repros post-fix across 300+ consecutive loop iterations run concurrently with the rest of this Sprint's other verification work (i.e. under genuine ambient multi-process CPU load on the same box, not an idle machine). Assertions unchanged throughout — every fix in this Sprint widened a wait budget or closed a registration-ordering window; no assertion was loosened, no t.Skip was added, coverage is unchanged."
    },
    {
      "timestamp": "2026-07-12T12:00:00Z",
      "category": "design",
      "title": "AC-S64c835-2-3: orphan-GC reap idempotency pinned at two layers",
      "detail": "Layer 1 (production primitive): added TestKillContainerProcesses_RepeatCallsAreIdempotent in internal/runtime/incus/incus_test.go — calls the real incusRuntime.KillContainerProcesses 4 times in a row against a fake runner that always returns pkill exit code 1 (models 'already reaped by a prior call, nothing left to match'), asserting every call returns nil AND every call dispatched the exact same well-formed pkill invocation — makes the previously-implicit 'pkill exit 1 is a safe no-op, repeatedly' contract (already documented in reapContainerClaude's doc comment) an explicit, checked assertion rather than something only ever exercised once, incidentally, by TestGCOrphans_ReapsInContainerClaude's follow-up ticks. Layer 2 (claudetui call site): TestGCOrphans_ReapsInContainerClaude's existing 2-extra-tick loop (which could ALREADY legitimately re-observe the just-shut-down orphan as still-live, since GCOrphans's cleanup is deliberately deferred to a later scan tick per its own doc comment) now has an explicit bounded assertion that every call recorded against the orphan's fakeContainerKiller across ALL ticks (zero or more — timing-dependent) has the identical (TERM, containerClaudeBin) shape, plus a documented rationale explaining WHY a repeat reap can legitimately happen there. Added a second, more direct test TestReapContainerClaude_RepeatCallsAreSafe that calls the claudetui-side reapContainerClaude helper itself 3 times for the same (repoID, branchID) and asserts all 3 calls are recorded with the same well-formed shape — pinning the contract at the palmux2-side call site too, independent of GCOrphans's own timing-dependent repeat-call frequency."
    },
    {
      "timestamp": "2026-07-12T12:00:00Z",
      "category": "verify",
      "title": "AC-S64c835-2-3 verification",
      "detail": "go test ./internal/runtime/incus/... -run TestKillContainerProcesses -v: both TestKillContainerProcesses and TestKillContainerProcesses_RepeatCallsAreIdempotent PASS. go test ./internal/tab/claudetui/ -run 'TestGCOrphans_ReapsInContainerClaude|TestReapContainerClaude_RepeatCallsAreSafe' -v: both PASS (real ptyhost processes, no leaks per teardown)."
    }
  ],
  "story_S64c835_3_implementation_notes": {
    "story": "S64c835-3",
    "title": "claude-agent orphan-GC (claudetui parity)",
    "summary": "Mirrors internal/tab/claudetui's S3f2658-3 GCOrphans (discover.go) into internal/tab/claudeagent, piggybacking the SAME store 10s scanPorts tick (no new loop). Closes the backlog gap left open at S862203-3's done time: a branch/repo closed while palmux2 was DOWN previously left its agent pipe-mode ptyhost running forever until the branch happened to be reopened.",
    "changes": [
      "internal/tab/claudeagent/discover.go: scanAgentRunDir now takes a skipLive(repoID,branchID,tabID) param (mirrors claudetui's scanRunDir) — referenced entries are excluded BEFORE any liveness dial, never touched. DiscoverAndRestore passes nil (unchanged behavior). New Manager.GCOrphans(ctx, isLive) scans + SHUTDOWNs every unreferenced LIVE ptyhost via a new sendAgentOrphanShutdown (raw dial + PipeClient.Shutdown, bypasses any live Agent/Client). New reapAgentContainerClaude mirrors claudetui's reapContainerClaude for in-container claude reap on GC-triggered SHUTDOWN (S52fc2c-4/S3f2658-4 parity), adapted to claudeagent's RuntimeResolver (runtime.ExecCommander instead of runtime.PTYCommander) via the same runtime.ContainerProcessKiller optional-capability type assertion.",
      "internal/store/sync_worktree.go: new AgentOrphanGC interface + Store.SetAgentOrphanGC + Store.gcAgentOrphans, wired into the SAME runPortScan 10s tick right after gcTuiOrphans. gcAgentOrphans reuses Store.isTuiTabLive as its isLive predicate — deliberately NOT a fresh isAgentTabLive: isTuiTabLive is not actually claude-tui-specific, it's a plain store.Tab(repoID,branchID,tabID) existence lookup, and a Claude AGENT tab's id (\"claude:claude\", \"claude:claude-2\", ...) lives in the exact same per-branch TabSet as every other tab type (claude-tui uses the same ids too — they're mutually exclusive per-tab, distinguished by Tab.Type \"claude\" vs \"claude-tui\", but GCOrphans only ever needs existence-by-id).",
      "internal/store/store.go: new agentGC AgentOrphanGC field alongside the existing tuiGC field.",
      "cmd/palmux/main.go: st.SetAgentOrphanGC(agentManager) wired right after the existing DiscoverAndRestore call for claudeagent, before st.Run(ctx) starts the scan loop (same ordering discipline as claudetui's wiring).",
      "internal/tab/claudeagent/discover_test.go (new): TestGCOrphans_ShutsDownUnreferenced_LeavesReferenced, TestGCOrphans_IDContainingDoubleUnderscore_SurvivesGC, TestGCOrphans_NilIsLive_IsNoOp — near-verbatim mirrors of claudetui/discover_test.go's equivalent tests, adapted to pipe-mode ptyhosts (ptyhost.ModePipe) and the fake_ndjson emitter (internal/ptyhost/testdata/fake_ndjson.go) claudeagent's own pipe-mode tests already use — real processes, not real claude.",
      "internal/store/agent_orphan_gc_test.go (new): TestGCAgentOrphans_NoopWithoutGC, TestGCAgentOrphans_CallsWiredGC, TestGCAgentOrphans_TuiAndAgentGC_BothWired — mirrors tui_orphan_gc_test.go, plus a joint test proving both reapers fire independently on the same tick (the actual production wiring shape)."
    ],
    "identity_discipline": "Same S3f2658-3 discipline: identity is read from the ptyhost status file's EXPLICIT RepoID/BranchID/TabID fields (already written by ptyhost.Config since S862203-3's pipe-mode support), never by re-splitting the \"__\"-joined Seed string. TestGCOrphans_IDContainingDoubleUnderscore_SurvivesGC proves a repoID containing the literal substring \"__\" (e.g. \"my__tool--ab12\") round-trips exactly and is never misattributed/killed across several GC ticks.",
    "gc_safety": "skipLive-before-dial: scanAgentRunDir consults skipLive (== isLive, threaded through from GCOrphans) immediately after resolving identity from the status file and BEFORE the pidAlive check, the socket dial, or the HELLO probe. A referenced entry is left completely untouched — never counted as live, never counted as cleaned, never dialed. This matters beyond efficiency: a pipe-mode ptyhost socket only tolerates ONE active connection (ptyhost.Server.replaceConn evicts whichever connection was previously active the instant a new one arrives), so a liveness-probe dial against an entry this same process already has a live Agent/Client attached to would silently evict that connection out from under it.",
    "backlog_closed": "This closes the gap explicitly flagged in cb2a60e (chore(S862203-3): mark story done + backlog (claudeagent orphan-GC gap)) and in discover.go's own pre-S64c835-3 doc comment (\"Unlike claudetui, there is no orphan-GC half here yet\").",
    "worktree_state_note": "The isolated worktree this story was implemented in was initially branched from an older `main` commit that PREDATED the S64c835 plan commit and the entire S862203 (pipe-mode ptyhost) merge — i.e. internal/tab/claudeagent/discover.go, launch.go, ptyclient.go, offsetstore.go etc. did not exist in the worktree at task start. Fast-forwarded (`git merge --ff-only autopilot/main/S64c835`) onto the branch this story was actually meant to build on before starting any work; the worktree had zero divergent commits so this was a clean, non-destructive fast-forward.",
    "verification": {
      "go_build": "pass (go build ./...)",
      "go_vet": "pass (go vet ./...)",
      "go_test": "pass: go test ./internal/tab/claudeagent/... ./internal/store/... ./cmd/... (non-race, real fake_ndjson processes for the 3 new GCOrphans tests, ~1s each, no PALMUX_SURVIVAL_SMOKE gate needed — same convention as this package's other fakeNDJSONBin-based tests, which are hermetic and fast, unlike the heavier real-binary/real-detached-process survival_gate_test.go tests)",
      "process_leak_check": "pgrep -af fake_ndjson after the full suite run showed no lingering processes"
    }
  }
}