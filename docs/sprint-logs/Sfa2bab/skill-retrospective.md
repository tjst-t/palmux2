# Skill Retrospective — Phase D milestone (Sprints S2b5691, Sfa2bab)

_Generated at milestone arrival. Failure/rework → SKILL diff, or an explicit reason not to._

**Signals this batch:** 6 (compromises 0, overlooked 2, reopens 0, ungrounded decisions 2/21 ≈ 9.5%, recurring process pattern 1 [×4 occurrences], concerns-sensor under-elicitation 1); concerns empty rate ~100% (high ⇒ sensor under-elicited this batch, not "all clear" — see #5)

| # | Signal (source) | What happened | Root cause | Diff proposal — or reason for deferral |
|---|---|---|---|---|
| 1 | process pattern · S2b5691-2 implement agent, S2b5691 verify agent (×3 occurrences across the batch) | A background sub-agent started a long-running process it needed to wait on (a real dev-server/E2E run, a real-incus acceptance test), then ended its own turn saying "waiting for task X to complete" / "standing by for a Monitor event" — but the completion notification never reached it (or the orchestrator), leaving the agent's work uncommitted and, in one case, an unattended real-incus instance running for 10+ minutes, actively conflicting with three unrelated live production containers on the host until the orchestrator intervened manually (killed the stuck process, cleaned up leftover containers/tmux sessions/orphaned ptyhost processes). | **SKILL/harness defect.** The Run/Verify phase instructions (`sprint-run.md`, `sprint-verify.md`, `sprint-auto.md`) tell agents to "loop until fixed" and don't warn against launching long/background operations and then passively waiting on an event the agent itself has no reliable way to be woken by. A competent agent following the current instructions literally (start the real test, wait for it) would still hit this — nothing in the current SKILL text says "poll with a bounded, explicit loop; never end your turn on a passive external wait for your own spawned background work." | Propose: add an explicit rule to `sprint-run.md` / `sprint-verify.md` (or a shared "background process discipline" note referenced by both) — spawned real-infrastructure processes must be (a) wrapped in an explicit `timeout`, (b) polled by the spawning agent itself in a bounded loop, never awaited via a passive/indefinite wait, and (c) torn down by the same agent turn that started them, even on failure/timeout. Target: `sprint/references/sprint-run.md` §Step 1, `sprint/references/sprint-verify.md` Phase 1.5, and possibly `sprint/references/test-discipline.md` as a new rule (real-machine test isolation/cleanup discipline is already implicit in several places but not stated as a hard rule with this specific "don't passively wait" framing). |
| 2 | overlooked · S2b5691 verifier | Run-phase self-report claimed 7/9 historical real-incus test logs passed for opencode-in-container reliability; the independent verifier re-read all 9 and found the true tally was 4/9, with 3+ distinct failure clusters. | Task-local for the *original* Plan-phase claim (a shallow sample generalized to a headline number), but the fact that a verifier catching this is load-bearing (it changed a Story's done_judgment) is exactly why `--with-verifier` exists and is working as designed — no SKILL defect here, the mesh caught it. | No diff — this is the mechanism functioning correctly. Noted for visibility, not for revision. |
| 3 | overlooked · Sfa2bab verifier | Self-report's AC status wasn't yet reflected in `docs/ROADMAP.json` (still `"pending"`) when the verifier ran, even though `verification-results.json` already had concrete pass/fail data — a reader skimming ROADMAP.json alone would have missed the AC-Sfa2bab-1-2 FAIL. | Task-local — the Run-phase agent correctly deferred ROADMAP status mutation to the orchestrator's Done phase per the normal division of labor; the verifier ran in the gap between Run and Done. Not a SKILL defect, just a timing artifact of when the verifier happened to run relative to the (thin, main-loop) Done phase. | No diff. Fixed immediately by the orchestrator's Done phase, as designed. |
| 4 | ungrounded decisions · 2/21 across S2b5691 + Sfa2bab decisions.json (~9.5%) | Two decisions lacked a VISION/DESIGN_PRINCIPLES reference. | Below the 30% warning threshold — noise, not a pattern. | No diff. |
| 5 | concerns-sensor under-elicitation · all Run-phase implement-agent prompts this batch | Implement-agent prompts asked for "out-of-scope issues", "escalations", and (for some) "concerns (in-scope, AC met, but something feels off) — empty list if genuinely none", but agents consistently folded everything into a single "escalations/concerns" bucket rather than a distinct concerns[] list, and nothing resembling the specific "AC-satisfying-but-off" shape (test-discipline.md's own example: a flow that reads as a hang, an abstraction pointing the wrong way) was ever returned separately this batch — everything reported was either a clear bug/gap (routed to backlog) or a load-bearing decision (routed to escalation). | Ambiguous — could be task-local (this batch's work genuinely had no "off but technically fine" moments) or a prompt-authoring gap (the orchestrator's own agent-launch prompts, not a canonical SKILL file, didn't isolate the concerns[] ask as its own clearly-labeled field per `sprint-run.md` Step 1's exact format). Given the instruction says a near-100% empty rate should be treated as "sensor under-elicited" by default, leaning SKILL-adjacent per the stated bias rule. | Deferral with a concrete note: next batch's orchestrator-authored implement-agent prompts should isolate the concerns[] ask as its own explicitly-labeled, single-purpose field (matching `sprint-run.md` Step 1's `[{note, theme}]` format exactly, separate from "escalations" and "backlog candidates"), rather than a merged prose section, and see if the empty rate changes. If it's still ~100% empty after that, the signal becomes stronger evidence of task-local reality rather than prompt friction. |
| 6 | process pattern · orchestrator-level | The orchestrator (this session) discovered the cross-container incident (#1) by manually inspecting `ps`/`incus list`/log files after a sub-agent's turn ended ambiguously, not because any structural signal flagged it. Nothing in the current autopilot flow prompts the orchestrator to sanity-check host state (stray processes, other containers) after a sub-agent that touches real infrastructure returns/times out. | SKILL/harness defect (adjacent to #1) — the orchestrator-side `autopilot-start.md` Sprint loop has no equivalent of "after resuming a stalled real-infra sub-agent, check host state directly" as a named step; it was improvised this session. | Propose: add a brief checklist item to `autopilot-start.md`'s Sprint loop (or `autopilot-operations.md`) — when a sub-agent's task-notification indicates it stopped mid-wait on its own background work ("standing by for X", "waiting for Y to complete" with no further notification), the orchestrator should directly inspect for orphaned real-infrastructure state (processes, containers, tmux sessions) before simply re-sending a message and trusting the agent to self-report cleanly. |

## Deferred (carried from prior milestones)

- (none — this is the first skill-retrospective.md generated for this project; no prior milestone had this artifact to carry forward from)
