# PLACEHOLDER — regenerated on first boot by palmux-state-init with the actual
# bootsector disk detected from the running VM (e.g. /dev/sda on virtio-scsi,
# /dev/vda on virtio-blk). Shipped so the template flake is self-consistent.
{ boot.loader.grub.device = "/dev/sda"; }
